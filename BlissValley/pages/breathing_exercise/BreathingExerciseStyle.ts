import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#ff91ed',
        alignItems: 'center',
        justifyContent: 'center'
    }
})

export default styles